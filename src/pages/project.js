import * as React from "react";

// components
import Layout from "../components/layout";

// markup
const ProjectPage = () => {
  return (
    <Layout>
      project page
    </Layout>
  )
}

export default ProjectPage;